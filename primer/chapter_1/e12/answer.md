What does the following for loop do? What is the final value of sum?

```cc
int sum = 0;
for (int i = -100; i <= 100; ++i)
sum += i;
```

This loop will sum the value of i in the `sum` variable and increment `i` by one for every iteration until the value of i is not less than or equal to 100. the final value is 0.
