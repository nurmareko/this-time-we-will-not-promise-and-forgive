Exercise 2.10: What are the initial values, if any, of each of the following variables?

std::string global_str;
int global_int;
int main()
{
    int local_int;
    std::string local_str;
}

answer:
  the global string and int variable will have default value of empty string and 0 respectively, the local variable will have undefined value.
