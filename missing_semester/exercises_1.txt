What does the -l flag to ls do? Run ls -l / and examine the output.
What do the first 10 characters of each line mean? (Hint: man ls)

answer:
the 'l' flag will make the result of ls become verbose, the first 10
lines indicate the file type and permissions, (-) for file and ( d )
for directory, followed by read, write execute permisions.

In the command find ~/Downloads -type f -name "*.zip" -mtime +30,
the *.zip is a “glob”. What is a glob? Create a test directory with
some files and experiment with patterns like ls *.txt, ls file?.txt,
and ls {a,b,c}.txt. See Pattern Matching in the Bash manual.

answer:
glob mean to match none or any characters at that exact position,
meaning the "*.zip" will match any file that start with any character
and will end with ".zip"

What’s the difference between 'single quotes', "double quotes", and
$'ANSI quotes'? Write a command that echoes a string containing a
literal $, a !, and a newline character. See Quoting.

answer:
+ single quotes will preserve the literal value, meaning if we give it
variable, it will now expand it to its value and just print it as it is
+ double quotes will handle variable expansion and will subtitute
variable with its value
+ ANSI quotes is treated as special kind of single quotes, with 
backslash-escaped characters is replaced as specified by the ANSI C
standard.

echo $'$ ! \n'

The shell has three standard streams: stdin (0), stdout (1), and
stderr (2). Run ls /nonexistent /tmp and redirect stdout to one file
and stderr to another. How would you redirect both to the same file?
See Redirections.

answer:

# to each file
ls /non /tmp > output.txt 2> error.txxt
# same fle
ls /non /tmp &> both.txt

$? holds the exit status of the last command (0 = success). && runs
the next command only if the previous succeeded; || runs it only if
the previous failed. Write a one-liner that creates /tmp/mydir only
if it doesn’t already exist. See Exit Status.

answer:
ls //tmp/mydir || mkdir /tmp/mydir

Why does cd have to be built into the shell itself rather than a
standalone program? (Hint: think about what a child process can and
cannot affect in its parent.)

answer:
Because a child process cannot change the working directory of its
parent process, meaning if cd were a standalone program, the OS would
run it in a new child proccess, update that child's directory, and then
end it, leaving the parent shell's directory completely unchagned.


Write a script that takes a filename as an argument ($1) and checks
whether the file exists using test -f or [ -f ... ]. It should print
different messages depending on whether the file exists. See Bash
Conditional Expressions.

answer:
if [ -f $1 ]; then echo 'exist'; else echo 'not exist'; fi


Save the script from the previous exercise to a file (e.g., check.sh).
Try running it with ./check.sh somefile. What happens? Now run
chmod +x check.sh and try again. Why is this step necessary?
(Hint: look at ls -l check.sh before and after the chmod.)

answer:
By default Unix-like OS treat newly created files as plain text, so if
we want to make it into executable we need to add the executable
permission by using the chmod +x


What happens if you add -x to the set flags in a script? Try it with
a simple script and observe the output. See The Set Builtin.

answer:


Write a command that copies a file to a backup with today’s date in
the filename (e.g., notes.txt → notes_2026-01-12.txt).
(Hint: $(date +%Y-%m-%d)). See Command Substitution.

answer:

