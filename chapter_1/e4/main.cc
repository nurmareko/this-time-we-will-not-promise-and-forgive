#include <iostream>

// A program that print the product of x and y
int main()
{
    int x, y;

    std::cout << "enter the value for x and y" << std::endl << ">> ";
    std::cin >> x >> y;

    std::cout << "The product of x * y is " << x * y << std::endl;

    return 0;
}
